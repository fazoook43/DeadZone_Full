# Manifests

Generated and hand-authored build manifests live here. The first architecture pass writes the active dry-run plan to `output/build_plan.json` and leaves this folder available for pinned release manifests later.
