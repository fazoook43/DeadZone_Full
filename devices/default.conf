# Dynamic partitions to dump from OTA payload.bin in Phase 1.
# Keep this broad for Xiaomi/HyperOS payloads; missing partitions are ignored after extraction.
DYNAMIC_PARTITIONS="system product system_ext vendor vendor_dlkm system_dlkm odm odm_dlkm mi_ext"
